#include <stdio.h>

int main( int argc, const char *argv[] )
{
    // check if there is '-h' flag in cmd,
    // if yes, read file's name and count new hash value
    // and print it

    // if no such flag, than '-i' for file to 'cure'
}
